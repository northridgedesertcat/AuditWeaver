id: csrf
name: Cross-Site Request Forgery
description: Detect legacy CSRF marker patterns.
attack_type: csrf
enabled: true
target_fields:
  - path
  - referrer
matchers:
  - type: regex
    library: csrf_regex
